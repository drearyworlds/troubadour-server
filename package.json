{
  "name": "troubadour-server",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "clean": "rd /s /q .\\dist && rd /s /q .\\bin",
    "build": "tsc",
    "build-electron": "npm run build && npm run copyHtml",
    "rebuild": "npm run clean && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "copyHtml": "copy .\\src\\index.html .\\dist\\",
    "start": "npm run build && node dist/app.js",
    "start-electron": "npm run build-electron && electron .",
    "make-electron-win": "npm run build-electron && electron-packager . troubadour-server --platform=win32 --arch=x64 --out=./bin"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/drearyworlds/songlistserver.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/drearyworlds/songlistserver/issues"
  },
  "homepage": "https://github.com/drearyworlds/songlistserver#readme",
  "devDependencies": {
    "@types/electron": "^1.6.10",
    "@types/express": "^4.16.1",
    "electron": "^12.0.1",
    "electron-packager": "^15.2.0",
    "tslint": "^5.12.1",
    "typescript": "^4.1.5"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.16.4",
    "mongoose": "^5.11.16",
    "tmi.js": "^1.7.3"
  }
}
